# Native mini-program shell

This directory is reserved for the unified native WeChat mini-program template.

The Phase 0 shell contains no AppID, merchant identifier, API URL, payment code, bootstrap implementation, or feature-flag implementation. Those values and behaviors must be supplied by later approved tasks through trusted runtime context and SaaS bootstrap configuration.
