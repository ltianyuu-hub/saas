{
  "name": "@saas/merchant",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.mjs"
  },
  "dependencies": {
    "@saas/database": "workspace:*",
    "@saas/identity-access": "workspace:*"
  }
}
